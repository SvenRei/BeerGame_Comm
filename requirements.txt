# SIGNAL -- Python dependencies.
#
# Training is CPU-bound (the per-step belief encode is sequential), so the CPU torch wheels are
# the default. Install torch from the CPU index FIRST, then this file:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
#     pip install -r requirements.txt
# On a GPU box, instead install the CUDA build for your toolkit, e.g.:
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# NOTE: auto_campaign2.sh (S0 bootstrap) installs this exact set on a fresh pod; this file is the
# single source of truth and is kept in sync with that pip line.

# Deep-learning engine (CPU wheels are sufficient; see note above)
torch>=2.0.0

# Multi-agent environment standard
pettingzoo>=1.23.0
gymnasium>=0.28.1

# Math & data arrays
numpy>=1.24.0
scipy>=1.10.0

# Statistics pipeline -- REQUIRED (all confirmatory inference delegates to these; scripts/c1_stats.py,
# scripts/confirmatory_v2.py, scripts/comm_stats.py, agents/eval_signal.py):
#   statsmodels -> Holm/BH multiple-comparison correction (multipletests)
#   scikit-learn -> R^2 scoring in the structural/distillation report
statsmodels>=0.14.0
scikit-learn>=1.3.0

# Tabular handling (results assembly / refs I/O)
pandas>=2.0.0

# Configuration management
hydra-core>=1.3.2
omegaconf>=2.3.0

# Experiment tracking (disabled on the pod via WANDB_MODE=disabled, but imported)
wandb>=0.15.0

# Plotting (result figures)
matplotlib>=3.7.0
seaborn>=0.12.2

# Tests
pytest>=7.0

# ---------------------------------------------------------------------------------------------
# OPTIONAL (install only if you use it):
#   pysr>=0.16.0          # symbolic search backend for scripts/distill_symbolic.py (pulls in Julia);
#                         # distill_symbolic falls back to a linear base-stock fit when PySR is absent.
# (torchvision / torchaudio are intentionally dropped -- nothing in this project imports them.)